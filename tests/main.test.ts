import { describe, it, beforeEach, expect, test } from 'vitest'
import { Launcher } from '../main.js';
import { exit } from 'node:process';



describe('Launcher', () => {
    // Launcher default
    let launcher: Launcher

    // beforeEach(()=>{
    //     launcher = new Launcher()
    // })


    // test('it should launch a runReadline by default', () => {
    //     launcher = new Launcher()
    // })

    // test("it should make the choice 0 then go random ",()=>{
    //     launcher = new Launcher([0])
    // })

    // test("it should kill the player",()=>{
    //     launcher = new Launcher([4,3,2,1,0])
    // })

})